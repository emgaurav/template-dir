# Git Template Directory Setup

This guide will help you set up a Git template directory containing pre-commit hooks which will be automatically applied to every new repository you clone or initialize.
